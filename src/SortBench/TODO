GUI design:

!!!!IMPORTANT!!!!:
- Function Sort() does not exists, so cannot be linked when NO_INLINE_STDARG
is set. This must be resolved in Sort Library code. NOTE: This probably has
to be resolved in "New Library..." dialog box of language-amigaos-c package!
Until then we have to compile without NO_INLINE_STDARG set.

o A cycle gadget to select between [Array] - [List]
o An input gadget for array/list size (DONE)
o An input gadget for value range 0-[   ] (DONE)
o An input gadget for item size (DONE)
o A button to start - stop benchmarking (DONE)

o A text to show what is being done at the moment (DONE)
o A progress bar (DONE)
o A table to show each sort method Name, and elapsed time (DONE)

o Calculate how many total progress steps
o Update progress bar at each step
o Write hook functions or functions that prohibit illegal values being
  entered in string gadgets.
o Add a sort in Forbid() mode.
o Selecting List should deactivate itemSize gadget
o Clicking "Start Benchmark" should deactive itself
o Clicking "Start Benchmark" should put mouse pointer in busy state
o Write Hash and Compare functions for different itemSizes
o Make the best benchmark value bold
o Write bubble help strings
o Return value of Sort() should be checked for memory errors
o There must be error requsters for failed memory allocations
o Prevent multiple instances of the program
o Create shortcut keys
o Create gadget cycle list
o Make opening of sort.library OS4 compatible.
/ Open sort.library after muimaster.library, so we can report errors in windows.
/ Write detailed documentation how to use the library.
o Test if the library compiles in morphos
o Pack the library source and sortbench source to be in one directory
- [OPTIONAL] Make the sort function a subtask so we can cancel it midway


Useful code:
//#ifdef __amigaos4__
//#define clearMem(a, b) SetMem(a, 0, b)
//#else
//#define clearMem(a, b) memset(a, 0, b)
//#endif

VOID printArray(LONG *arr, LONG size);
VOID printList(struct MinList *list);

/****************************
 * NOTE: Debug Stuff REMOVE *
 ****************************/
///printArray
VOID printArray(LONG *arr, LONG size)
{
  UBYTE buffer[8];
  UBYTE result[256];
  LONG i;

  result[0] = 0;

  for (i = 0; i < size; i++)
  {
    sprintf(buffer, "%ld", arr[i]);
    if (i != 0) strcat(result, ", ");
    strcat(result, buffer);
  }

  puts(result);
}
///
///printList
VOID printList(struct MinList *list)
{
  UBYTE buffer[8];
  UBYTE result[1024] = {0};
  BOOL firstTime = TRUE;

  struct MyItem* i;
  for (i = (struct MyItem*) list->mlh_Head; i->node.mln_Succ; i = (struct MyItem*) i->node.mln_Succ)
  {
    sprintf(buffer, "%ld", i->val);
    if (firstTime) firstTime = FALSE; else strcat(result, ", ");
    strcat(result, buffer);
  }
  puts(result);
}
///

/****************************
 * pragmas/sort.h           *
 ****************************/


Buzdolabı 421 (560lt 4440TL) inox 3820TL
TV 80
31 Mart'a kadar
